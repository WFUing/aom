export function convertToBlock(block: Record<string, any>): string {
    if (block['kind'] === 'comp_block') {
        let compBlocks = block['compBlocks'] as Record<string, any>

    }

    if (block['kind'] === 'provider_block') {

    }

    if (block['kind'] === 'data_block' || block['kind'] === 'resource_block') {

    }

    return ""
}